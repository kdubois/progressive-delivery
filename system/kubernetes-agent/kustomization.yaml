apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-gitops

resources:
  - rbac.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - secret.yaml  # Git-ignored file with actual secrets
                 # Create from template: cp secret.yaml.template secret.yaml
